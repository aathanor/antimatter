baseurl = "https://florin.antimaterie.ro"
languageCode = "en-us"
Title = "Antimatters"
# Copyright notice. This is displayer in the footer.
copyright = "&copy; Florin Cojocariu"

# Google Analytics API key.
googleAnalytics = "G-ESHWTJ7NNN"

# enable get info from git commits, such as file modification time.
# so we don't have to manually update it.
enableGitInfo = true

paginate = 10

[params]
  # Hide the date on posts
  # hideDate = true
  # Hide last modified date on posts
  hideModifiedDate = true
  # Hide the word count on posts and in the posts list
  hideWordCount = true
  # Hide the reading time on posts and in the posts list
  # hideReadingTime = true
  custom_css = ["/css/custom.css"]
  # Social accounts. Link to these accounts are displayed in the header and
  # footer
  twitter = "aathanor"
  facebook = "ivan.zubeldian.7"
  instagram = "aathanor"
  # Disqus shortname
  disqus = ""
  author = "Your Name"
  authorwebsite = "example.com"
  avatar = "/path/to/avatar"
  contact = "Your contact link (ex. mailto:foo@example.com)"
  bio = "Your short bio"
  # Short subtitle/tagline. This is displayed in the header.
  subtitle = "thoughts & feelings"
  # Logo image. This is displayed ogp image.
  logo = "./assets/logo.jpg"
  favicon = "./assets/fav.jpg"

[[menu.main]]
    name = "Blog"
    url = "/"
    weight = 1

[[menu.main]]
    name = "About"
    url = "/about"
    weight = 2

[[menu.main]]
    identifier = "theme"
    name = "/theme"
    weight = 3

[[menu.main]]
    parent = "theme"
    name = "creating-a-new-theme"
    url = "/posts/creating-a-new-theme"
    weight = 1

[[menu.main]]
    parent = "theme"
    name = "migrate-from-jekyll"
    url = "/posts/migrate-from-jekyll"
    weight = 2

[[menu.main]]
    name = "Tags"
    url = "/tags"
    weight = 4

[related]
  # Only include matches with rank >= threshold. This is a normalized rank between 0 and 100.
  threshold = 80

  # To get stable "See also" sections we, by default, exclude newer related pages.
  includeNewer = false

  # Will lower case keywords in both queries and in the indexes.
  toLower = false

  [[related.indices]]
  name = "keywords"
  weight = 150
  [[related.indices]]
  name  = "author"
  toLower = true
  weight = 30
  [[related.indices]]
  name  = "tags"
  weight = 100
  [[related.indices]]
  name  = "date"
  weight = 10
  pattern = "2017"
